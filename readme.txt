git init

git add *

git commit -m "initial commit"

git remote add origin <git repo url>

git branch - m main

git push -u origin main

git checkout -b develop main

git add *

git status

git commmit -m "added readme.txt"

git branch

git push -u origin develop

--> create new file mytestfile

git add mytestfile

git status

git reset HEAD

git add *

git commit -m "added mytestfile"

git log

